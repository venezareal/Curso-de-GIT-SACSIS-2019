#include <stdio.h>

int soma(int a, int b){
	in soma_num = a + b;
	return soma_num;
}
int sub(int a, int b){
	in sub_num = a - b;
	return sub_num;
}

int main(){
	printf("Soma dos dois números: %d\n", soma(5,5));
		printf("Soma dos dois números: %d\n", sub(5,5));
	return 0;
	}
